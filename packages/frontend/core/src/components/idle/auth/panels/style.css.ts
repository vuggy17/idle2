import { style } from '@vanilla-extract/css';

export const signInForm = style({
  width: '100%',
});

export const signInFormBtnGroup = style({
  width: '100%',
  justifyContent: 'flex-end',
});

export const createAccountGuest = style({
  display: 'block',
  textAlign: 'center',
});
